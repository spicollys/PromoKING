<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mpoo.promoking">

    <application
        android:name=".infra.ui.PromoKINGApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".publicacao.ui.InfoPublicacaoEstComercialActivity"></activity>
        <activity android:name=".publicacao.ui.EditarPublicacaoEstComercialActivity" />
        <activity android:name=".publicacao.ui.ListarPublicacaoEstComercialActivity" />
        <activity android:name=".usuario.ui.CadastroActivity" />
        <activity android:name=".infra.ui.MainActivityEstabelecimentoComercial" />
        <activity android:name=".usuario.ui.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".infra.ui.MainActivityCliente" />
    </application>

</manifest>